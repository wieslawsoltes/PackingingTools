# PackagingTools.Core

Core orchestration, configuration, and packaging engine abstractions. Hosts per-OS workers, policy enforcement, signing integrations, telemetry, and persistence services consumed by GUI, CLI, SDK, and automation surfaces.
